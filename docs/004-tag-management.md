# 004 タグ管理

**フェーズ:** MVP
**ステータス:** 未着手
**依存:** 001, 003
**被依存:** 005, 007, 014

---

## 概要

タグのグローバル管理（作成・編集・削除）、カードへのタグ付け、タグによるフィルタリング・検索を実装する。タグはデッキ横断でグローバル管理される。

---

## Todo

### データ層
- [x] タグ作成（`INSERT INTO tags`）— lib/database/tags.ts
- [x] タグ一覧取得（全グローバルタグ・使用枚数付き）
- [x] タグ更新（name, color）
- [x] タグ削除（card_tags の紐付きも削除）
- [x] カードにタグ付与（`INSERT OR IGNORE INTO card_tags`）
- [x] カードからタグ削除
- [x] カードのタグ一覧取得（JOIN）
- [x] タグでデッキ横断カード取得（getCardIdsByTagId）
- [x] Zustand タグストア（`useTagStore`）— store/tags.ts

### 画面 / UI
- [x] `app/tags/index.tsx` — タグ管理画面
  - [x] タグ一覧（名前・色・使用枚数）
  - [x] タグ作成（名前・プリセットカラー選択）
  - [x] タグ編集・削除（確認ダイアログ付き）
- [x] 設定タブからタグ管理画面へのリンク
- [x] タグフィルター UI（デッキ詳細・学習開始前）— 005/007 チケットで実装

### i18n
- [x] タグ関連テキストの翻訳キー追加

---

## 技術メモ

- タグ色は HEX 文字列で保存
- `card_tags` は (cardId, tagId) の複合主キー
