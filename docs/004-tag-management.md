# 004 タグ管理

**フェーズ:** MVP
**ステータス:** 未着手
**依存:** 001, 003
**被依存:** 005, 007, 014

---

## 概要

タグのグローバル管理（作成・編集・削除）、カードへのタグ付け、タグによるフィルタリング・検索を実装する。タグはデッキ横断でグローバル管理される。

---

## Todo

### データ層
- [ ] タグ作成（`INSERT INTO tags`）
- [ ] タグ一覧取得（全グローバルタグ）
- [ ] タグ更新（name, color）
- [ ] タグ削除（card_tags の紐付きも削除）
- [ ] カードにタグ付与（`INSERT INTO card_tags`）
- [ ] カードからタグ削除
- [ ] カードのタグ一覧取得（JOIN）
- [ ] タグでカード検索・フィルタリング
- [ ] タグでデッキ横断学習セッション用カード取得
- [ ] Zustand タグストア（`useTagStore`）

### 画面 / UI
- [ ] `app/tags/index.tsx` — タグ管理画面
  - [ ] タグ一覧（名前・色・使用枚数）
  - [ ] タグ作成（名前・色選択）
  - [ ] タグ編集・削除
- [ ] タグフィルター UI（デッキ詳細・学習開始前）
- [ ] タグカラーピッカー

### i18n
- [ ] タグ関連テキストの翻訳キー追加

---

## 技術メモ

- タグ色は HEX 文字列で保存
- `card_tags` は (cardId, tagId) の複合主キー
