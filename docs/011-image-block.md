# 011 画像ブロック

**フェーズ:** v1.0
**ステータス:** 完了
**依存:** 005
**被依存:** なし

---

## 概要

カードエディタで画像ブロックを追加・表示できるようにする。端末ローカルから画像を選択してカードに埋め込む。

---

## Todo

### 画像選択・保存
- [x] `expo-image-picker` インストール
- [x] 端末フォトライブラリからの画像選択
- [x] 選択した画像をアプリ固有ストレージにコピー（`expo-file-system`）
- [x] URI スキーム設計（`local://images/{uuid}.{ext}`）
- [x] カード削除時の画像ファイル削除

### 画像ブロック UI（エディタ）
- [x] 画像ブロックコンポーネント（エディタ内プレビュー）
- [x] 画像選択ボタン（未選択時）
- [x] 画像変更・削除操作
- [x] alt テキスト入力欄

### 画像ブロック UI（プレビュー / 学習画面）
- [x] 画像ブロックのフル表示
- [x] ピンチズームで拡大
- [x] alt テキストのアクセシビリティ対応

### ストレージ管理
- [x] 未使用画像の検出・削除（孤立ファイルのクリーンアップ）
- [x] 画像ファイルサイズ制限（目安: 5MB）
- [x] JSON エクスポート時の画像処理（base64 埋め込み or 別ファイル）

### i18n
- [x] 画像ブロック関連テキストの翻訳キー追加

---

## 技術メモ

- `expo-image-picker` のパーミッション設定（`app.json` に追記）
- 画像は `FileSystem.documentDirectory + 'images/'` に保存
- エクスポート時は base64 埋め込みを基本とする
