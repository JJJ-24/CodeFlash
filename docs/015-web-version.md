# 015 Web 版（カード作成専用）

**フェーズ:** v1.1
**ステータス:** 未着手
**依存:** 005
**被依存:** 016

---

## 概要

Expo Web を使って同一コードベースからブラウザ版を提供する。機能はカード作成・編集・プレビューに限定（学習機能なし）。データ連携は JSON エクスポート/インポートで行う。

---

## Todo

### Web 対応
- [ ] `npm run web` で基本動作確認
- [ ] Web 非対応コンポーネントの条件分岐（`Platform.OS === 'web'`）
  - [ ] `react-native-webview`（コード実行）は Web では代替実装
  - [ ] `expo-sqlite` は Web では `expo-sqlite/legacy` or `wa-sqlite` を検討
  - [ ] `react-native-reanimated` の Web 動作確認
  - [ ] ファイルシステム API の Web 代替

### Web 専用 UI 調整
- [ ] レスポンシブレイアウト（デスクトップ幅対応）
- [ ] マウス操作 UX 調整（ホバー効果等）
- [ ] キーボードショートカット（Web標準）

### 機能制限
- [ ] 学習画面を Web では非表示
- [ ] iCloud 同期を Web では非表示
- [ ] Web 限定バナー表示（「学習はモバイルアプリで」）

### データ連携
- [ ] JSON エクスポートボタン（ブラウザダウンロード）
- [ ] JSON インポート（ファイル選択 or ドラッグ&ドロップ）
- [ ] インポート時のデータ検証

### デプロイ
- [ ] Expo EAS でのWebビルド設定
- [ ] ホスティング先検討（Vercel / Cloudflare Pages 等）

### i18n
- [ ] Web 版専用テキストの翻訳キー追加

---

## 技術メモ

- Web 版は Pro 機能（016 チケット）として提供
- `Platform.select` で Web/モバイルの実装を切り替え
