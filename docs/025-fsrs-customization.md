# 025 FSRSカスタマイズ

**フェーズ:** v1.0
**ステータス:** MVP実装済み（v1.0リリース対象）
**依存:** 006, 007, 016
**被依存:** なし

---

## 概要

FSRS アルゴリズムのパラメータをユーザーが調整できるようにする。
学習スタイルや目的に応じて間隔反復の挙動をカスタマイズできる Pro 機能。

---

## Todo

### v1.0 実装範囲（MVP・実装済み）
- [x] `@react-native-community/slider` インストール（Dev Build 再ビルド必要）
- [x] `store/settings.ts` に `fsrsDesiredRetention`・`fsrsPreset` を追加（AsyncStorage 永続化）
- [x] `lib/fsrs.ts` を設定値読み込みに変更（Pro 未購入時は 90% 固定）
- [x] 設定画面に「FSRSカスタマイズ」セクション追加（Pro ゲーティング付き）
- [x] プリセット選択 UI（試験対策 95% / 標準 90% / 長期記憶 80% / カスタム）
- [x] 目標保持率スライダー（70%〜99%・1%刻み）
- [x] プリセット⇔スライダーの双方向連動（値がプリセットと一致したら自動でプリセット選択）
- [x] i18n 翻訳キー追加（ja.json / en.json）

### v1.1 以降に延期
- [ ] 最大間隔日数の上限設定
- [ ] 新規カードの1日あたり上限枚数（学習セッションのキュー管理に手を入れる必要あり）
- [ ] 復習カードの1日あたり上限枚数（同上）
- [ ] デッキ単位での個別設定（DB スキーマ拡張必要）
- [x] カスタムプリセットの保存（DB スキーマ拡張必要）
- [ ] `enable_short_term` ON/OFF 切替（学習画面の再出題ロジック追加が必要）

---

## 技術メモ

- `ts-fsrs` ライブラリは `FSRSParameters` でパラメータを受け取れる（`lib/fsrs.ts` 参照）
- 設定値は `useSettingsStore`（AsyncStorage）に保存
- パラメータ変更は既存の `reviews` データには遡及しない（新規学習から適用）

## 検討事項

### `enable_short_term` の扱い
現在 `lib/fsrs.ts` は `enable_short_term: false` で初期化しており、短期ステップ（同日中の複数回レビュー）がスキップされる。これにより：
- 新規カードが最初のレビュー後すぐに Review 状態になる
- 2回目の「再考」がラプス（ミス）としてカウントされる
- Anki などの標準 FSRS とは挙動が異なる

`enable_short_term: true` にすれば標準 FSRS に近づくが、同日中に同じカードが複数回出る UI が必要になる（学習セッション内での再出題ロジックの追加）。Pro 設定として「短期ステップ ON/OFF」を選択できるようにすることを検討する。
