# 028 Pro 機能 追加候補

**フェーズ:** v1.1 以降
**ステータス:** 候補（未着手）
**依存:** 016
**被依存:** ―

---

## 概要

買い切り Pro 版の訴求力を高めるための追加機能候補を一覧化する。いずれも **運用コストゼロ（サーバー不要・LLM API不要）** で実装可能な機能のみを掲載する。

優先度・実装順は未確定。ペイウォール画面の「今後追加予定」に掲載する機能の候補プールとしても機能する。

関連既存チケット：
- 027 ホームウィジェット（既存・別チケット）
- 014 iCloud 同期（既存・別チケット）
- 018 SQL / C++ 実行（既存・別チケット）
- 025 FSRS カスタマイズ（既存・別チケット）

---

## 候補 1: デッキ・カードのカスタマイズ

ペイウォールに掲載予定の機能。デッキとカードの見た目を自由にカスタマイズできるようにする。

### サブ機能
- **デッキのアイコン**（複数候補から選択 or Ionicons 任意指定）
- **デッキのカラーテーマ**（プリセット + カスタムカラー）
- **カード表示のテーマカラー**（背景・枠線・コードブロック）
- **フォント変更**（プリセットの数種類から選択）

### 設計方針メモ：「色付きアイコン」として価値を出す

デッキ名の先頭にアイコンを置くだけなら、ユーザーが絵文字をデッキ名に入れることでほぼ同じ体験ができる（無料・即時）。
そのため、**専用アイコン機能の価値は「カラーテーマと組み合わせて『色付きアイコン』として表現できること」に置く**。

- アイコンは Ionicons 等の **単色ベクター**を採用
- デッキカラーをアイコンの fill 色に適用 → 絵文字では実現不可能な「**統一カラーリング**」を実現
- ウィジェット（027）と連動する際もタイル状にクリアに表示可能
- ベクター描画のため、デッキ詳細ヘッダーで**大サイズ装飾**としても利用できる

絵文字との差別化ポイントを明確にした上で機能を訴求する。

### 実装メモ
- `decks` テーブルに `iconName`・`colorHex` カラム追加（マイグレーション必要）
- `useSettingsStore` にグローバル `cardThemePreference`・`fontFamilyPreference` を追加
- 既存の `useTheme()` を拡張して `customColor` を返せるようにする
- フォントは expo-font で同梱（日本語対応必須）

### 想定難易度
中程度。マイグレーション + 設定 UI + 適用箇所の特定で数日規模。

---

## 候補 2: 高度な通知・リマインダー

現状の通知リマインダー（023）はアプリ全体で1日1回のみ。Pro 版では細かい設定を可能にする。

### サブ機能
- **デッキ別リマインダー**（特定のデッキの due 数で通知）
- **タグ別リマインダー**（タグ X のカードが N 枚溜まったら通知）
- **時間帯指定**（朝の通学時間・夜の就寝前など複数時刻）
- **曜日別 ON/OFF**（土日は通知しない 等）
- **スマート通知**（学習を忘れている日数で文言を変える）

### 実装メモ
- `expo-notifications` の `scheduleNotificationAsync` を複数 identifier で管理
- `notification_schedules` テーブル新設（deckId / tagId / hour / minute / weekday）
- 設定画面に通知スケジュール一覧 + 追加/編集モーダルを設置

### 想定難易度
中程度。通知 API は既に組み込み済みなので、UI とスケジューリングロジックが主作業。

---

## 候補 3: PDF / 印刷出力

学習記録・カード一覧を PDF として出力できるようにする。紙の単語帳として印刷する用途や、学習履歴の保存用途。

### サブ機能
- **デッキの全カードを PDF 化**（表面・裏面の2ペーン構成）
- **学習記録の月次レポート PDF**（学習日数・カード数・グレード分布）
- **印刷用 CSS** で iOS の印刷ダイアログから直接印刷

### 実装メモ
- `expo-print` ライブラリ使用
- HTML テンプレートをコード生成 → `printToFileAsync` で PDF 化
- `Sharing.shareAsync` で保存 / メール送信

### 想定難易度
低〜中。expo-print のみで完結し、ライブラリ追加リスクも低い。

---

## 候補 4: 詳細な検索機能

現状の全文検索（022）に検索フィルター機能を追加する。

### サブ機能
- **タグ複合検索**（タグ A AND タグ B、タグ A OR タグ B）
- **日付範囲フィルター**（作成日・更新日・最終学習日）
- **習熟度フィルター**（未学習・学習中・習熟済み）
- **デッキ絞り込み**（複数デッキ選択）
- **検索条件の保存**（よく使う条件を「クイックフィルター」として保存）

### 実装メモ
- `lib/database/cards.ts` の `searchCards` を拡張
- 検索画面（`app/search.tsx`）にフィルタードロワー追加
- クイックフィルター用に `saved_filters` テーブル新設

### 想定難易度
中程度。SQL クエリビルダーの複雑度が上がる点に注意。

---

## 優先度の目安

訴求力 × 実装コストのバランス：

| 候補 | 訴求力 | 実装コスト | 優先度 |
|----|----|----|----|
| 1. デッキ・カードのカスタマイズ | 高（見た目の差別化） | 中 | ★★★ |
| 2. 高度な通知・リマインダー | 中 | 中 | ★★ |
| 3. PDF / 印刷出力 | 低〜中（特定層に刺さる） | 低 | ★★ |
| 4. 詳細な検索機能 | 中（パワーユーザー向け） | 中 | ★★ |

ペイウォール表示優先候補：**1（デッキ・カードのカスタマイズ）**。

---

## Todo

各候補から正式着手するものが決まった段階で、個別チケットに分割する。
- [ ] v1.1 〜 v2.0 の機能ロードマップ確定
- [ ] 候補ごとに優先度を決定
- [ ] 着手する候補を選んだら独立したチケットファイルに分割

---

## 技術メモ

- ここに列挙する機能は **すべて運用コストゼロ**（サーバー・LLM API・課金 SDK 外を要しない）
- AI 生成（020）・デッキ共有リンク（026）・Web 版（015）は運用コスト発生のため別チケットで管理
- 各候補の詳細仕様は着手前に再評価する（現時点では大枠のみ）
