# v1.10.1 リリースノート

App Store Connect の「このバージョンの新機能」欄に貼り付け用。

対象コミット: `e76a075`（v1.10.0 の最終コミット）以降

> ⚠️ App Store Connect は一部の記号を弾く（丸囲みの i・全画面・再生などの装飾記号、および山括弧）。
> 本文ではそれらを使わないこと（過去に差し戻した経緯あり）。

---

## 日本語

```
デッキ土台の複数持ちと1日の目標枚数を中心に、HTML プレビューの安定性を大きく改善したアップデートです。

【新機能・改善】
・デッキの土台を複数持てるように（Pro）：HTML/CSS 土台と SQL 初期化にそれぞれ名前を付けて複数作成でき、コードブロックごとにどれを使うか選べます
・1日の目標枚数を設定できるように：達成時のお知らせ、学習タブと学習完了画面での進捗表示、未達成のときだけ届くリマインダー通知に対応しました
・学習を中断して戻ったとき、最後に見ていたカードにフォーカスするようになりました
・通知が鳴らない理由を通知設定画面に表示するようになりました（アプリ側がオフ／iOS 側で未許可）
・テキストブロック内のマークダウンの複数行コードを横スクロールに対応：折り返さずコードの形が保たれます
・コードブロックとデッキ土台の設定説明を i ボタンで開閉するようにしました（既定は閉じた状態）
・HTML 画像ライブラリを土台の一覧へ移動：土台が1つも無いデッキでも開けるようになりました
・デッキ編集で HTML/CSS 土台を SQL 初期化より上に配置しました
・土台の一覧をキーボードで操作できるようになりました（J / K で移動、Return で開く、Delete で削除、N で追加）

【不具合の修正】
・HTML プレビューでリンクやフォームを操作するとアプリが終了することがある不具合を修正
・フォームの送信先が空のときにプレビューが真っ白になる不具合を修正（ブラウザと同じくリロードになります）
・コードの実行が終わらないまま止まったままになることがある不具合を修正
・土台・初期化のシートの背後に、押せない保存ボタンが透けて見える問題を修正
・テキストブロックで行頭に空白を入れると、意図せずコードとして表示される問題を修正

その他、内部のライブラリ更新と細かな表示の調整を含みます。
```

---

## English

```
This update centers on multiple deck stages and a daily card goal, plus major stability fixes for the HTML preview.

[ New & Improved ]
• Decks can now hold multiple stages (Pro): create several named HTML/CSS stages and SQL setups, and pick which one each code block uses
• Added a daily card goal: get a notice when you reach it, see your progress on the Study tab and the session summary, and receive a reminder only when the goal is still unmet
• Returning to a session you left now focuses the card you were last on
• The notification settings screen now tells you why notifications will not fire (turned off in the app, or not allowed in iOS)
• Multi-line code in text blocks now scrolls horizontally instead of wrapping, so the shape of the code is preserved
• Explanations for code block and deck stage settings now open and close with an i button (closed by default)
• The HTML image library moved to the stage list, so you can open it even when a deck has no stages
• HTML/CSS stages now appear above SQL setup in deck editing
• The stage list can now be operated from a keyboard (J / K to move, Return to open, Delete to remove, N to add)

[ Bug fixes ]
• Fixed a crash that could occur when tapping a link or submitting a form in the HTML preview
• Fixed the preview turning blank when a form had no destination (it now reloads, as a browser would)
• Fixed code execution that could stay stuck without ever finishing
• Fixed the save button showing through behind the stage / setup sheets even though it was not tappable
• Fixed leading spaces in a text block being shown as code unintentionally

Also includes internal library updates and minor display adjustments.
```

---

## App Review メモ（App Store Connect → App Reviewに関する情報 → メモ）

> ※ ID / Password はリポジトリには記載していません。App Store Connect に貼り付けるときに手動で埋めてください。

```
今回（v1.10.1）は v1.10.0 の HTML / CSS プレビューまわりの安定性改善が中心で、
デッキ土台の複数持ちと1日の目標枚数を追加したアップデートです。

【課金について】
新規の課金商品・サブスクリプションはありません（Pro は v1.3.0 で承認済みの買い切り1商品のまま）。
デッキ土台の複数持ちは既存の Pro 機能の拡張で、新しい課金導線はありません。

【外部通信について（v1.6.0 から方針変更なし）】
- JavaScript / TypeScript / Python / SQL / HTML / CSS：すべてデバイス内で完結。外部サーバーへの通信なし
  （Python は Pyodide を CDN から初回ロード、SQL は sql.js をバンドル）
- C++：Wandbox API（https://wandbox.org/）へコードを送信して実行結果を取得します。
  ユーザーが C++ ブロックの実行ボタンを押したときのみ通信が発生します。

今回、新しい通信先の追加はありません。

【主な修正】
HTML プレビュー内でリンクのタップやフォームの送信を行うとアプリが終了することがある不具合を修正しました
（利用している WebView ライブラリ側の不具合で、修正済みバージョンへ更新しています）。

---

## 主なテスト手順
1. デッキの土台の複数持ち（Pro）
   - デッキ編集 → HTML/CSS 土台 → 右上のプラスで土台を2つ以上作成し、それぞれ名前と内容を入力
   - カードのコードブロックで「使うデッキ土台」から1つを選び、実行結果が選んだ土台に応じて変わることを確認
   - SQL 初期化でも同様に複数作成・選択できることを確認
2. 1日の目標枚数
   - 設定 → 学習 → 1日の目標枚数を設定 → 学習して達成時にお知らせが出ることを確認
   - 学習タブと学習完了画面に進捗が表示されることを確認
3. HTML プレビューの安定性
   - HTML のコードブロックにリンクとフォームを書いて実行 → 全画面プレビューでタップしてもアプリが終了しないことを確認
4. 通知
   - 設定 → 通知でスケジュールを作成 → アプリ側の通知をオフにすると理由が表示されることを確認

### Sandbox テスト用 Apple ID（既存購入の検証用）
ID: __SANDBOX_TESTER_EMAIL__
Password: __SANDBOX_TESTER_PASSWORD__

### 補足
- 課金は RevenueCat 経由で処理されます（v1.3.0 から変更なし、新規 IAP 商品なし）。
- ユーザー登録・サーバー側のアカウント管理はなし。
- データは端末ローカル（SQLite）と、ユーザー自身の iCloud にのみ保存されます（iCloud 同期オン時）。
```
