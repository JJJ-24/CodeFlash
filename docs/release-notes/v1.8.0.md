# v1.8.0 リリースノート

App Store Connect の「このバージョンの新機能」欄に貼り付け用。

---

## 日本語

```
ハードウェアキーボード操作の全面強化と、統計「学習の記録」の追加、テキスト装飾の強化を中心としたアップデートです。

【新機能・改善】
・ハードウェアキーボード対応を全面刷新：ほぼ全画面にショートカットを追加し、矢印・Esc・Tab・Delete などに対応。「?」でその画面のショートカット一覧を6カテゴリーで表示。仕組みをネイティブ化し、キーボード利用時のタップ不応や復帰時のフリーズを解消。一部の既存ショートカットの割り当ても見直しました（最新は各画面の「?」でご確認いただけます）
・統計に「学習の記録」を追加：学習履歴（草グラフ）をタップすると、最長連続・累計・継続率・バッジなどの歩みを表示。自己ベスト更新中はバッジで通知
・統計のドーナツグラフに正答率・学習日数・平均時間を追加
・テキスト装飾を強化：ハイライト（マーカー）記法と複数色、下線（アンダーライン）に対応。装飾パレットをブロック直下のインライン表示にして iPhone/iPad 両対応（見出しはレベル循環）。⌘B／⌘I／⌘U などの装飾ショートカットにも対応
・画像ブロックに表示サイズ（小／中／大）を追加
・カード編集画面に複製ボタンを追加
・カード一覧に「ここから学習」を追加：カードを右スワイプ（またはフォーカスして ⇧Space）すると、そのカードから一覧の最後までを続けて学習できます
・手動並べ替えのロックを追加：ホーム／カード一覧の手動ソート時にロックアイコンでドラッグ並べ替えを固定でき、その状態でスワイプ操作（ここから学習・アーカイブ・削除）が使えます
・iCloud 同期にリモートデータの最終更新時刻を表示。データ復元のデッキ一覧に「最終編集」日時を表示
・カード検索で「/」キーからすばやく検索欄へカーソル移動

【不具合の修正】
・学習画面から開いた編集画面で、下部のボタンが初回に反応しないことがある不具合を修正
・画面構成が原因で一部画面のタップが不安定になる不具合を修正
・iPad で特定の操作後にヘッダーが縮む／ステータスバーが消える不具合を修正
・マークダウン見出しで日本語の上部が欠けることがある不具合を修正
・カード編集で、ブロックの編集中にツールバーや補助入力パレットをタップすると編集が解除されることがある不具合を修正

その他、ショートカット表示やラベルの調整、ダークモードのコード背景や補助入力パレットの表示など細かな改善を含みます。
```

---

## English

```
This update focuses on greatly expanded hardware keyboard support, a new "Learning Record" in Stats, and enhanced text decoration.

[ New & Improved ]
• Completely revamped hardware keyboard support: shortcuts on nearly every screen, with Arrow / Esc / Tab / Delete keys. Press "?" to see that screen's shortcuts grouped into 6 categories. The mechanism is now native, fixing unresponsive taps and freezes when a keyboard is in use. Some existing shortcut assignments were also revised (see each screen's "?" for the latest)
• Added "Learning Record" to Stats: tap the activity heatmap to see your journey — longest streak, totals, consistency rate, and badges. A badge appears while you're setting a new record
• Added correct rate, study days, and average time to the Stats donut chart
• Enhanced text decoration: highlight (marker) syntax with multiple colors and underline; the decoration palette now appears inline right under the block for both iPhone and iPad (headings cycle through levels); added decoration shortcuts like ⌘B / ⌘I / ⌘U
• Added display size (Small / Medium / Large) to image blocks
• Added a duplicate button to the card editor
• Added "Study from here" to the card list: swipe a card right (or focus it and press ⇧Space) to study from that card through to the end of the list
• Added a lock for manual reordering: while Home or the card list is in manual sort, tap the lock icon to lock drag-reordering, which enables swipe actions (study from here, archive, delete) in that state
• iCloud sync now shows the last-updated time of the remote data; the restore screen's deck list shows a "last edited" time
• In card search, press "/" to quickly move the cursor to the search field

[ Bug fixes ]
• Fixed an issue where bottom buttons could be unresponsive on the first tap in an editor opened from the study screen
• Fixed an issue where taps could become unstable on some screens due to the view structure
• Fixed an issue on iPad where the header could shrink or the status bar could disappear after certain actions
• Fixed an issue where the top of Japanese characters could be clipped in Markdown headings
• Fixed an issue where editing could be dismissed when tapping the toolbar or symbol palette while editing a block in the card editor

Plus minor text/label adjustments (dark-mode code background, symbol palette layout, etc.) and other small improvements.
```

---

## App Review メモ（App Store Connect → App Reviewに関する情報 → メモ）

> ※ ID / Password はリポジトリには記載していません。App Store Connect に貼り付けるときに手動で埋めてください。

```
今回（v1.8.0）はハードウェアキーボード操作の全面強化・UI改善・不具合修正が中心のアップデートです。
新規の課金商品・サブスクリプションは追加していません（Pro は v1.3.0 で承認済みの「買い切り1商品」のまま）。
外部通信の方針は v1.7.1 から変更ありません。

【コード実行の通信について（v1.6.0 から方針変更なし）】
- JavaScript / TypeScript / Python / SQL：すべてデバイス内で完結。外部サーバーへの通信なし。
  （Python は Pyodide を CDN から初回ロード、SQL は sql.js をバンドル）
- C++：Wandbox API（https://wandbox.org/）へコードを送信して実行結果を取得します。
  ユーザーが C++ ブロックの実行ボタンを押したときのみ通信が発生します。

C++ / SQL の実行は Pro 機能です。

---

## v1.8.0 の変更点
主な変更は、ハードウェアキーボード操作のネイティブ化と全画面へのショートカット追加（矢印/Esc/Tab/Delete 対応、
「?」でショートカット一覧を表示）、統計の「学習の記録」（草グラフタップで継続・バッジ表示）とドーナツグラフの指標追加、
テキスト装飾の強化（ハイライト記法・下線・インライン装飾パレット・装飾ショートカット）、画像ブロックの表示サイズ、
カード一覧の「ここから学習」・手動並べ替えロック、
および学習画面から開いた編集画面のボタン不応・iPad のヘッダー/ステータスバー不具合・ブロック編集中のツールバー操作で編集が解除される不具合の修正です。
新たな外部通信・新規 IAP はありません。

### 主なテスト手順
1. **ハードウェアキーボード（任意・Bluetooth/Magic Keyboard 接続時）**
   - 各画面で「?」を押すとショートカット一覧が表示されることを確認
   - J/K でフォーカス移動、矢印/Esc/Tab が効くこと、キーボード接続中でも画面タップが正常に反応することを確認
2. **統計「学習の記録」**
   - 統計タブ → 学習履歴（草グラフ）をタップ → 最長連続・累計・バッジなどのモーダルが開くことを確認
   - ドーナツグラフに正答率・学習日数・平均時間が表示されることを確認
3. **テキスト装飾**
   - カード編集でテキストブロックを編集 → 直下の装飾パレットで太字/ハイライト等が適用できることを確認（iPhone/iPad 両方）
4. **画像ブロックの表示サイズ**
   - 画像ブロックで 小／中／大 を切り替え、表示サイズが変わることを確認
5. **編集画面のボタン反応（回帰確認）**
   - 学習画面 → カード編集を開き、下部のボタンが初回タップで反応することを確認
6. **カード一覧の操作（任意）**
   - カードを右スワイプで「ここから学習」が開始できることを確認
   - 手動ソート時にロックアイコンで並べ替えをロックし、その状態で左スワイプ（アーカイブ／削除）が使えることを確認

### Sandbox テスト用 Apple ID（既存購入の検証用）
ID: __SANDBOX_TESTER_EMAIL__
Password: __SANDBOX_TESTER_PASSWORD__

### 補足
- 課金は RevenueCat 経由で処理されます（v1.3.0 から変更なし、新規 IAP 商品なし）。
- ユーザー登録・サーバー側のアカウント管理はなし。
- データは端末ローカル（SQLite）と、ユーザー自身の iCloud にのみ保存されます（iCloud 同期オン時）。
```
