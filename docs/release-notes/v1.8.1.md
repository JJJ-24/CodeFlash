# v1.8.1 リリースノート

App Store Connect の「このバージョンの新機能」欄に貼り付け用。

---

## 日本語

```
新しいカードテーマの追加と、操作性・応答性の改善を中心としたアップデートです。

【新機能・改善】
・カードテーマに「アプリコット」「ターコイズ」の2色を追加（Pro）
・ステータスバー（画面上部の時刻部分）のタップで一覧を先頭へスクロールする iOS 標準機能に対応
・カード枚数が非常に多いデッキで、カード一覧のフィルター・並び順の切替がすばやく反応するように改善
・カード検索：検索結果のタップや統計・編集の操作で、そのカードがフォーカス表示されるように（カード一覧と同じ挙動）
・タグ管理に並べ替えロックを追加（手動ソート時にドラッグ並べ替えを固定でき、その状態でスワイプ削除が使えます。⌘L ショートカット対応）
・設定・統計・検索でも、余白タップでキーボードのフォーカスを解除できるように
・ショートカット一覧で ⇧U／⇧D（最上部/最下部へスクロール）をできるように

【不具合の修正】
・素早く2回タップすると同じ画面が2枚重なって開くことがある不具合を修正
・一部の設定（並べ替えロック・言語・フィルター状態など）が JSON エクスポート/インポートで復元されない不具合を修正
・カード編集のタグ選択で、「配色テーマ追従」「白黒」のタグの枠線やドットが背景に溶けて見えにくい不具合を修正
・Dynamic Island 搭載機で一部画面のヘッダー高さがわずかにずれる不具合を修正

その他、余白やアイコン位置の調整など細かな改善を含みます。
```

---

## English

```
This update focuses on new card themes and improvements to usability and responsiveness.

[ New & Improved ]
• Added two new card themes: "Apricot" and "Turquoise" (Pro)
• Supported the standard iOS behavior of tapping the status bar (the clock area at the top) to scroll lists back to the top
• Faster response when switching filters or sort order in the card list on decks with a very large number of cards
• Card search: tapping a result or using the stats/edit actions now focuses that card (same behavior as the card list)
• Added a reorder lock to tag management (in manual sort, lock drag-reordering to enable swipe-to-delete; ⌘L shortcut supported)
• You can now tap empty space to dismiss keyboard focus on the Settings, Stats, and Search screens as well
• Added ⇧U / ⇧D shortcuts to scroll to the top / bottom

[ Bug fixes ]
• Fixed an issue where tapping twice quickly could open the same screen twice
• Fixed an issue where some settings (reorder lock, language, filter states, etc.) were not restored via JSON export/import
• Fixed an issue in the card editor's tag selector where the border and dot of "Follow theme" and "Black/White" tags could blend into the background
• Fixed an issue where the header height was slightly off on devices with a Dynamic Island

Plus minor adjustments to spacing, icon positions, and other small improvements.
```

---

## App Review メモ（App Store Connect → App Reviewに関する情報 → メモ）

> ※ ID / Password はリポジトリには記載していません。App Store Connect に貼り付けるときに手動で埋めてください。

```
今回（v1.8.1）は新しいカードテーマの追加と操作性・応答性の改善が中心のマイナーアップデートです。
新規の課金商品・サブスクリプションは追加していません（Pro は v1.3.0 で承認済みの「買い切り1商品」のまま）。
外部通信の方針は v1.6.0 から変更ありません。

【コード実行の通信について（v1.6.0 から方針変更なし）】
- JavaScript / TypeScript / Python / SQL：すべてデバイス内で完結。外部サーバーへの通信なし。
  （Python は Pyodide を CDN から初回ロード、SQL は sql.js をバンドル）
- C++：Wandbox API（https://wandbox.org/）へコードを送信して実行結果を取得します。
  ユーザーが C++ ブロックの実行ボタンを押したときのみ通信が発生します。

C++ / SQL の実行は Pro 機能です。

---

## v1.8.1 の変更点
主な変更は、カードテーマ2色追加（Pro）・ステータスバータップで先頭へスクロール・
カード一覧のフィルター/ソート切替の応答改善・タグ管理の並べ替えロックと、
ダブルタップでの画面二重表示・JSONエクスポートの設定キー漏れなどの不具合修正です。
新たな外部通信・新規 IAP はありません。

### 主なテスト手順
1. **ステータスバータップ**
   - 一覧画面（ホーム・カード一覧など）をスクロール後、画面上部のステータスバーをタップ → 先頭へ戻ることを確認
2. **カードテーマ（Pro）**
   - 設定 → 表示 → カードテーマで「アプリコット」「ターコイズ」が選択でき、配色が反映されることを確認
3. **タグ管理の並べ替えロック**
   - タグ管理でソートを手動に → ロックアイコンで並べ替えをロック → その状態で左スワイプ（削除）が使えることを確認
4. **画面遷移（回帰確認）**
   - デッキなどを素早く2回タップしても同じ画面が2枚開かないことを確認

### Sandbox テスト用 Apple ID（既存購入の検証用）
ID: __SANDBOX_TESTER_EMAIL__
Password: __SANDBOX_TESTER_PASSWORD__

### 補足
- 課金は RevenueCat 経由で処理されます（v1.3.0 から変更なし、新規 IAP 商品なし）。
- ユーザー登録・サーバー側のアカウント管理はなし。
- データは端末ローカル（SQLite）と、ユーザー自身の iCloud にのみ保存されます（iCloud 同期オン時）。
```
