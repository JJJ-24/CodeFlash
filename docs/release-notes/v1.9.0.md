# v1.9.0 リリースノート

App Store Connect の「このバージョンの新機能」欄に貼り付け用。

---

## 日本語

```
学習タイマー（Pro）と Pro の1週間無料体験、統計の「今日のサマリー」を中心としたアップデートです。

【新機能・改善】
・学習タイマーを追加（Pro）：残り時間をパイ型で表示するタイマーです（1〜60分）。学習画面を開いている間だけ進み、複数のデッキをまたいでも継続します。「今日は10分だけ学習しよう」というときに便利です。表示は「常に／開始時だけ／オフ」の3択で、円を隠してミニマルな数字だけの表示も選べます
・ポモドーロ（学習→休憩の繰り返し）に対応（Pro）：学習と休憩を自動で繰り返します。休憩を0分にすれば、休憩なしの連続セットにもできます
・Pro を1週間無料でお試しできる「体験」を追加：ペイウォールから一度だけ開始でき、期間中はすべての Pro 機能を利用できます（体験中は残り日数をバッジで表示）
・統計に「今日のサマリー」を追加：今日の消化率をバーで表示し、タップすると評価の内訳（正答率・初学習・平均回答時間）をドーナツで確認できます
・統計タブの各セクションを折りたためるように（開閉の状態は保存されます）
・通知スケジュールを左スワイプで削除できるように

【不具合の修正】
・iPad の分割表示／Stage Manager で、隣のアプリがキーボードを出すと画面が崩れる・ズレることがある不具合を修正
・統計の「新規」ブロックの枚数が、過去7日間の作成グラフと食い違うことがある不具合を修正
・評価別ランキングで、アーカイブ済みのカードをグレー表示するように

その他、「次回予定」→「次回復習」への用語統一など、細かな改善を含みます。
```

---

## English

```
This update centers on a Study Timer (Pro), a 1-week free Pro trial, and a new "Today's Summary" in Stats.

[ New & Improved ]
• Added a Study Timer (Pro): a pie-style countdown (1–60 min) that runs only while you're studying and continues across decks — great for "just 10 minutes today." Choose how it appears (Always / At start / Off), or hide the circle for a minimal number-only display
• Added Pomodoro (repeating study → break) (Pro): automatically alternates study and break periods. Set the break to 0 minutes for continuous, break-free sets
• Added a 1-week free Pro trial: start it once from the paywall to use all Pro features for the trial period (a badge shows the days remaining)
• Added "Today's Summary" to Stats: a bar shows today's completion rate; tap it to see a grade breakdown donut (correct rate, first-time cards, average answer time)
• You can now collapse each section on the Stats tab (the open/closed state is remembered)
• You can now swipe left to delete a notification schedule

[ Bug fixes ]
• Fixed an issue on iPad (Split View / Stage Manager) where the screen could break or shift when a neighboring app showed the keyboard
• Fixed an issue where the "New" block count in Stats could disagree with the past-7-days creation graph
• Archived cards are now shown grayed out in the per-grade ranking

Plus minor improvements such as unified wording ("Next due" → "Next review"), and other small fixes.
```

---

## App Review メモ（App Store Connect → App Reviewに関する情報 → メモ）

> ※ ID / Password はリポジトリには記載していません。App Store Connect に貼り付けるときに手動で埋めてください。

```
今回（v1.9.0）は学習タイマー（Pro）・Pro の1週間無料体験・統計の「今日のサマリー」追加が中心のアップデートです。

【課金について】
新規の課金商品・サブスクリプションは追加していません（Pro は v1.3.0 で承認済みの「買い切り1商品」のまま）。
今回追加した「1週間無料体験」は、StoreKit のサブスクリプションや導入価格（イントロ価格）ではなく、
アプリ内で Pro 機能を1週間だけ無料開放する仕組みです。
再体験を防ぐため、体験の開始時刻をユーザー自身の iCloud（NSUbiquitousKeyValueStore）に保存します
（外部サーバーへの送信はありません）。購入導線（買い切り1商品）は従来どおりです。

外部通信の方針は v1.6.0 から変更ありません。

【コード実行の通信について（v1.6.0 から方針変更なし）】
- JavaScript / TypeScript / Python / SQL：すべてデバイス内で完結。外部サーバーへの通信なし。
  （Python は Pyodide を CDN から初回ロード、SQL は sql.js をバンドル）
- C++：Wandbox API（https://wandbox.org/）へコードを送信して実行結果を取得します。
  ユーザーが C++ ブロックの実行ボタンを押したときのみ通信が発生します。

C++ / SQL の実行は Pro 機能です。

---

## v1.9.0 の変更点
主な変更は、学習タイマー（Pro・パイ型カウントダウン／数字表示／表示3択）とポモドーロ（学習→休憩の繰り返し・Pro）、
Pro の1週間無料体験（アプリ内での期間限定開放。iCloud KV で再体験を防止）、
統計の「今日のサマリー」（消化率バー＋評価内訳ドーナツ）・セクション折りたたみ、
通知スケジュールの左スワイプ削除、
および iPad マルチウィンドウでの画面崩れ・統計「新規」枚数の不整合などの不具合修正です。
新たな外部通信・新規 IAP はありません。

### 主なテスト手順
1. **学習タイマー（Pro）**
   - 設定 → 学習設定でタイマーを ON にし、時間（1〜60分）と「円/残り時間の表示」を設定
   - 学習を開始 → タイマーが進むこと、別デッキの学習に移っても継続することを確認
   - 時間切れで終了アラートが出ること、「続ける」で再スタートすることを確認
2. **ポモドーロ（Pro）**
   - 学習設定で休憩時間を設定 → 学習終了後に休憩に切り替わり、休憩後に再び学習に戻ることを確認
   - 休憩を 0 分にすると休憩なしの連続セットになることを確認
3. **Pro の1週間無料体験**
   - ペイウォールから体験を開始（開始は一度きり）→ すべての Pro 機能が使えること、残り日数バッジが出ることを確認
   - 期間経過後は無料状態に戻ること、アプリ再インストールでは再体験できないことを確認
4. **統計「今日のサマリー」**
   - 統計タブ上部の「消化率」バーをタップ → 評価内訳ドーナツ（正答率・初学習・平均時間）のシートが開くことを確認
   - 各セクションのタイトル行タップ／右端 chevron で折りたたみできることを確認
5. **iPad マルチウィンドウ（回帰確認・任意）**
   - Split View で本アプリと別アプリ（メモ等）を並べ、別アプリでキーボードを出しても本アプリの画面が崩れないことを確認

### Sandbox テスト用 Apple ID（既存購入の検証用）
ID: __SANDBOX_TESTER_EMAIL__
Password: __SANDBOX_TESTER_PASSWORD__

### 補足
- 課金は RevenueCat 経由で処理されます（v1.3.0 から変更なし、新規 IAP 商品なし）。
- ユーザー登録・サーバー側のアカウント管理はなし。
- データは端末ローカル（SQLite）と、ユーザー自身の iCloud にのみ保存されます（iCloud 同期オン時）。
- 本バージョンはネイティブ変更（iCloud Key-Value ストア用の自作モジュール＋entitlement）を含むため、配布ビルドは EAS で再ビルドしています。
```
